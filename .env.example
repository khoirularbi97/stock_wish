# Example environment variables for local development
# Copy this file to .env.local and fill with your Supabase project values.
# Do NOT commit your real keys. Use .env.local (which is ignored) for secrets.

# Supabase (public) - required for client-side usage
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Supabase (server-only) - only set this on the server (do NOT expose in the browser)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Other NEXT.js env examples
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4028/api

# How to use
# 1. Copy this file:  cp .env.example .env.local
# 2. Fill in the actual values from your Supabase project settings:
#    https://app.supabase.com/project/_/settings/api
# 3. Restart the dev server: npm run dev
